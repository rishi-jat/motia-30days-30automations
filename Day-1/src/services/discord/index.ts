import { sendNotification } from './send-notification'

/**
 * Discord service following DDD pattern
 * Handles all Discord-related operations
 */
export const discordService = {
    sendNotification,
}
